---
description: Lightweight settings for sqm test rendering
---

## Rendering preferences

- Use AA1 for routine smoke and test renders because AA3 is unnecessarily slow.
- Use AA2 only when AA1 produces visibly insufficient quality; do not use AA3 or higher unless the user explicitly requests a high-quality render.
